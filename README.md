# College Admission Form

A simple college admission form created using HTML5 and basic CSS.

## Features
- User-friendly admission form layout
- Uses multiple input types (text, email, date, radio buttons, checkboxes)
- Dropdown menu for course selection
- Styled using basic CSS for clean layout and spacing

## Technologies Used
- HTML5
- CSS3